import React, {Component, Fragment} from "react";
import {hot} from "react-hot-loader";

class App extends Component {
	render() {
		return (
			<div>Welcome to Relativity6 React Base</div>
		);
	}
}

export default hot(module)(App);
